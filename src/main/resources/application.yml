server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/edu_db?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # dev ??? update / prod ?????? none
    show-sql: true
    properties:
      hibernate:
        format_sql: true


